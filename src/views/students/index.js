import React from 'react';
import { Box, Typography } from '@mui/material';

// ==============================|| STUDENTS PAGE ||============================== //

const Students = () => {
  return (
    <Box sx={{ p: 3 }}>
      <Typography variant="h1" sx={{ fontSize: '2.5rem', fontWeight: 600, color: '#333' }}>
        Students View
      </Typography>
    </Box>
  );
};

export default Students;
